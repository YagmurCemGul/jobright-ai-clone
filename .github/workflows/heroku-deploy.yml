name: Deploy Backend to Heroku

on:
  push:
    branches:
      - main
    # We could add a path filter to run only when backend code changes,
    # but for now, running on every main push is simpler and a more general starting point.
    # paths:
    #   - 'server.js'
    #   - 'routes/**'
    #   - 'models/**'
    #   - 'config/**'
    #   - 'package.json'
    #   - 'package-lock.json'

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to Heroku
        uses: akhileshns/heroku-deploy@v3.12.12
        with:
          heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
          heroku_app_name: ${{ secrets.HEROKU_APP_NAME }}
          heroku_email: ${{ secrets.HEROKU_EMAIL }}
        env:
          # Eğer Heroku uygulamanızda başka environment variable'lar varsa,
          # You can also add them here. For example:
          # HD_MONGO_URI: ${{ secrets.MONGO_URI }}
          # HD_JWT_SECRET: ${{ secrets.JWT_SECRET }}